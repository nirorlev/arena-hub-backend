<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeatom.guidecore.mapper.GcUserFabulousMapper">
    <resultMap id="baseResultMap" type="com.threeatom.guidecore.entity.GcUserFabulous">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="eventId" column="eventId"/>
        <result property="videoId" column="video_id"/>
        <result property="targetUserId" column="target_user_id"/>
        <result property="userId" column="user_id"/>
        <result property="updateTime" column="update_time"/>
        <result property="createTime" column="create_time"/>
        <result property="commentId" column="comment_id"/>
    </resultMap>

    <select id="getUserFabulous" resultMap="baseResultMap">
        select id,user_id,event_id,video_id,target_user_id,comment_id
        from gc_user_fabulous
        where 1=1
        <if test="entity.userId != null">
            and user_id = #{entity.userId}
        </if>
        <if test="entity.eventId != null">
            and event_id = #{entity.eventId}
        </if>
        <if test="entity.videoId != null">
            and video_id = #{entity.videoId}
        </if>
        <if test="entity.targetUserId != null">
            and target_user_id = #{entity.targetUserId}
        </if>
        <if test="entity.commentId != null">
            and comment_id = #{entity.commentId}
        </if>
    </select>

    <select id="getEventFabulousNum" resultType="java.lang.Integer">
        select count(1)
        from gc_user_fabulous
        where target_user_id = #{targetUserId}
        and event_id = #{eventId}
        and comment_id = #{commentId}
    </select>
    <select id="getVideoFabulousNum" resultType="java.lang.Integer">
        select count(1)
        from gc_user_fabulous
        where target_user_id = #{targetUserId}
        and video_id = #{eventId}
        and comment_id = #{commentId}
    </select>
    <select id="getEventFabulousNumList" resultMap="baseResultMap">
        select target_user_id AS "targetUserId",user_id,event_id as eventId,video_id,target_user_id,user_id,id
        from gc_user_fabulous
        where event_id = #{eventId} and comment_id = target_user_id
        and target_user_id in
        <foreach item="targetId" collection="targetUserIdList" open="(" separator="," close=")">
            #{targetId}
        </foreach>
        group by id
    </select>
</mapper>