<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeatom.guidecore.mapper.GcUserNoteMapper">

	<select id="countNoteForVideo" resultType="Integer">
		select count(1) from gc_user_note where  video_id = #{videoId}
	</select>

</mapper>
